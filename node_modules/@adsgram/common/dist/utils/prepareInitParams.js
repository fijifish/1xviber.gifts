export function prepareInitParams(params) {
    const initParams = { blockId: params.blockId };
    if (params.debug !== undefined)
        initParams.debug = params.debug;
    if (params.debugBannerType !== undefined)
        initParams.debugBannerType = params.debugBannerType;
    return initParams;
}
